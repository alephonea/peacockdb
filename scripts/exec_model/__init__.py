"""Python prototype of the batch-partitioned execution model.

Not production code: an emulation whose purpose is to settle the execution model — the
scheduling rule, the driver split, and the trait set — before any Rust exists. See
`README.md` and `llm-wiki/tasks/batch_partitioned_executor.md`.
"""
