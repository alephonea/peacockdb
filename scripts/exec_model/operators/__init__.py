"""Pandas-backed implementations of the node set's operators.

pandas is restricted to the subset that has a `cudf` C++ counterpart; the rules and the
known divergences are stated at the top of `frame.py`.
"""
