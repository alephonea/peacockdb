GpuSortExec: expr=[revenue@2 DESC], fetch=20, time_us=1005
  GpuProjectExec: expr=[c_custkey@0 as c_custkey, c_name@1 as c_name, sum(lineitem.l_extendedprice * Int64(1) - lineitem.l_discount)@7 as revenue, c_acctbal@2 as c_acctbal, n_name@4 as n_name, c_address@5 as c_address, c_phone@3 as c_phone, c_comment@6 as c_comment], time_us=271
    GpuAggregateExec: group_by=[c_custkey, c_name, c_acctbal, c_phone, n_name, c_address, c_comment], aggr=[sum(lineitem.l_extendedprice * Int64(1) - lineitem.l_discount)], time_us=6976
      GpuProjectExec: expr=[c_custkey@1 as c_custkey, c_name@2 as c_name, c_address@3 as c_address, c_phone@4 as c_phone, c_acctbal@5 as c_acctbal, c_comment@6 as c_comment, l_extendedprice@7 as l_extendedprice, l_discount@8 as l_discount, n_name@0 as n_name], time_us=314
        GpuCoalesceBatchesExec, time_us=1
          GpuHashJoinExec: join_type=Inner, on=[(n_nationkey@0, c_nationkey@3)], projection=[1, 2, 3, 4, 6, 7, 8, 9, 10], time_us=1261
            GpuScanExec: table=nation, projections=[n_nationkey, n_name], time_us=843
            GpuCoalesceBatchesExec, time_us=1
              GpuHashJoinExec: join_type=Inner, on=[(o_orderkey@7, l_orderkey@0)], projection=[0, 1, 2, 3, 4, 5, 6, 9, 10], time_us=1360
                GpuCoalesceBatchesExec, time_us=1
                  GpuHashJoinExec: join_type=Inner, on=[(c_custkey@0, o_custkey@1)], projection=[0, 1, 2, 3, 4, 5, 6, 7], time_us=1047
                    GpuScanExec: table=customer, projections=[c_custkey, c_name, c_address, c_nationkey, c_phone, c_acctbal, c_comment], time_us=168767
                    GpuCoalesceBatchesExec, time_us=1
                      GpuFilterExec: predicate=o_orderdate@2 >= 1993-10-01 AND o_orderdate@2 < 1994-01-01, projection=[0, 1], time_us=172
                        GpuScanExec: table=orders, projections=[o_orderkey, o_custkey, o_orderdate], time_us=27443
                GpuCoalesceBatchesExec, time_us=1
                  GpuFilterExec: predicate=l_returnflag@3 = R, projection=[0, 1, 2], time_us=551
                    GpuScanExec: table=lineitem, projections=[l_orderkey, l_extendedprice, l_discount, l_returnflag], time_us=36912

# sync_floor_us = cost of the measurement itself (empty timed region: clock + cudaStreamSynchronize on an idle stream). Each node time INCLUDES one PER OUTPUT PARTITION, since a node line is the Σ over its partitions; a node at or below its own floor (sync_floor_us x partitions) is unresolved, not cheap. Do not subtract it.
# nodes_total_us = Σ of the per-node times above; total_us = the whole query, end to end (parse + plan + serialize + node walk + materialize)
# build_profile = how the harness itself was compiled. total_us MINUS nodes_total_us is that Rust; records from different profiles are not comparable on it. Node times are device work and barely move.
# allocator = the rmm device resource the node times were taken under. Without a pool every cuDF intermediate is a cudaMalloc/cudaFree round trip, charged to the node that allocated it — so it inflates the largest-output nodes hardest and moves the PROFILE, not only the scale. Records under different allocators are not comparable, node for node or in total.
# shared_work_charged_to = which p<k> sub-line carries work a node does once for all of its output partitions. A hash repartition concatenates its input and scatters it in a single operation, and that time is billed to p0 — so p0 standing far above its siblings is the accounting, not skew. Sub-lines sum to their node line either way. Written whether or not this plan has a repartition.
build_profile=benchmarks opt-level=3
allocator=rmm-pool initial=113.4GiB max=126.7GiB of 133.4GiB free on a discrete device
shared_work_charged_to=p0
sync_floor_us=1
nodes_at_or_below_floor=5/18
nodes_total_us=246927
total_us=253227
