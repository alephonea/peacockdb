GpuProjectExec: expr=[CAST(sum(lineitem.l_extendedprice)@0 AS Float64) / 7 as avg_yearly], time_us=96
  GpuAggregateExec: group_by=[], aggr=[sum(lineitem.l_extendedprice)], time_us=394
    GpuCoalesceBatchesExec, time_us=1
      GpuHashJoinExec: join_type=Inner, on=[(p_partkey@2, l_partkey@1)], filter=CAST(l_quantity@0 AS Decimal128(30, 15)) < Float64(0.2) * avg(lineitem.l_quantity)@1, projection=[1], time_us=1073
        GpuProjectExec: expr=[l_quantity@1 as l_quantity, l_extendedprice@2 as l_extendedprice, p_partkey@0 as p_partkey], time_us=68
          GpuCoalesceBatchesExec, time_us=1
            GpuHashJoinExec: join_type=Inner, on=[(p_partkey@0, l_partkey@0)], projection=[0, 2, 3], time_us=1531
              GpuCoalesceBatchesExec, time_us=1
                GpuFilterExec: predicate=p_brand@1 = Brand#23 AND p_container@2 = MED BOX, projection=[0], time_us=1083
                  GpuScanExec: table=part, projections=[p_partkey, p_brand, p_container], time_us=32269
              GpuScanExec: table=lineitem, projections=[l_partkey, l_quantity, l_extendedprice], time_us=40250
        GpuProjectExec: expr=[CAST(0.2 * CAST(avg(lineitem.l_quantity)@1 AS Float64) AS Decimal128(30, 15)) as Float64(0.2) * avg(lineitem.l_quantity), l_partkey@0 as l_partkey], time_us=671
          GpuAggregateExec: group_by=[l_partkey], aggr=[avg(lineitem.l_quantity)], time_us=2898
            GpuScanExec: table=lineitem, projections=[l_partkey, l_quantity], time_us=32239

# sync_floor_us = cost of the measurement itself (empty timed region: clock + cudaStreamSynchronize on an idle stream). Each node time INCLUDES one PER OUTPUT PARTITION, since a node line is the Σ over its partitions; a node at or below its own floor (sync_floor_us x partitions) is unresolved, not cheap. Do not subtract it.
# nodes_total_us = Σ of the per-node times above; total_us = the whole query, end to end (parse + plan + serialize + node walk + materialize)
# build_profile = how the harness itself was compiled. total_us MINUS nodes_total_us is that Rust; records from different profiles are not comparable on it. Node times are device work and barely move.
build_profile=benchmarks opt-level=3
sync_floor_us=1
nodes_at_or_below_floor=3/14
nodes_total_us=112575
total_us=115915
