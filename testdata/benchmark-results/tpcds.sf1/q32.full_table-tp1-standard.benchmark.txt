GpuProjectExec: expr=[sum(catalog_sales.cs_ext_discount_amt)@0 as excess discount amount], time_us=9
  GpuGlobalLimitExec: skip=0, fetch=100, time_us=1
    GpuAggregateExec: group_by=[], aggr=[sum(catalog_sales.cs_ext_discount_amt)], time_us=219
      GpuCoalesceBatchesExec, time_us=1
        GpuHashJoinExec: join_type=Inner, on=[(i_item_sk@1, cs_item_sk@1)], filter=CAST(cs_ext_discount_amt@0 AS Decimal128(30, 15)) > Float64(1.3) * avg(catalog_sales.cs_ext_discount_amt)@1, projection=[0], time_us=478
          GpuCoalesceBatchesExec, time_us=1
            GpuHashJoinExec: join_type=Inner, on=[(cs_sold_date_sk@0, d_date_sk@0)], projection=[1, 2], time_us=266
              GpuProjectExec: expr=[cs_sold_date_sk@1 as cs_sold_date_sk, cs_ext_discount_amt@2 as cs_ext_discount_amt, i_item_sk@0 as i_item_sk], time_us=28
                GpuCoalesceBatchesExec, time_us=1
                  GpuHashJoinExec: join_type=Inner, on=[(i_item_sk@0, cs_item_sk@1)], projection=[0, 1, 3], time_us=444
                    GpuCoalesceBatchesExec, time_us=1
                      GpuFilterExec: predicate=i_manufact_id@1 = 977, projection=[0], time_us=166
                        GpuScanExec: table=item, projections=[i_item_sk, i_manufact_id], time_us=5396
                    GpuScanExec: table=catalog_sales, projections=[cs_sold_date_sk, cs_item_sk, cs_ext_discount_amt], time_us=11496
              GpuCoalesceBatchesExec, time_us=1
                GpuFilterExec: predicate=d_date@1 >= 2000-01-27 AND d_date@1 <= 2000-04-26, projection=[0], time_us=154
                  GpuScanExec: table=date_dim, projections=[d_date_sk, d_date], time_us=18245
          GpuProjectExec: expr=[CAST(1.3 * CAST(avg(catalog_sales.cs_ext_discount_amt)@1 AS Float64) AS Decimal128(30, 15)) as Float64(1.3) * avg(catalog_sales.cs_ext_discount_amt), cs_item_sk@0 as cs_item_sk], time_us=94
            GpuAggregateExec: group_by=[cs_item_sk], aggr=[avg(catalog_sales.cs_ext_discount_amt)], time_us=582
              GpuCoalesceBatchesExec, time_us=1
                GpuHashJoinExec: join_type=Inner, on=[(d_date_sk@0, cs_sold_date_sk@0)], projection=[2, 3], time_us=479
                  GpuCoalesceBatchesExec, time_us=1
                    GpuFilterExec: predicate=d_date@1 >= 2000-01-27 AND d_date@1 <= 2000-04-26, projection=[0], time_us=152
                      GpuScanExec: table=date_dim, projections=[d_date_sk, d_date], time_us=16458
                  GpuScanExec: table=catalog_sales, projections=[cs_sold_date_sk, cs_item_sk, cs_ext_discount_amt], time_us=11474

# sync_floor_us = cost of the measurement itself (empty timed region: clock + cudaStreamSynchronize on an idle stream). Each node time INCLUDES one PER OUTPUT PARTITION, since a node line is the Σ over its partitions; a node at or below its own floor (sync_floor_us x partitions) is unresolved, not cheap. Do not subtract it.
# nodes_total_us = Σ of the per-node times above; total_us = the whole query, end to end (parse + plan + serialize + node walk + materialize)
# build_profile = how the harness itself was compiled. total_us MINUS nodes_total_us is that Rust; records from different profiles are not comparable on it. Node times are device work and barely move.
# allocator = the rmm device resource the node times were taken under. Without a pool every cuDF intermediate is a cudaMalloc/cudaFree round trip, charged to the node that allocated it — so it inflates the largest-output nodes hardest and moves the PROFILE, not only the scale. Records under different allocators are not comparable, node for node or in total.
# shared_work_charged_to = which p<k> sub-line carries work a node does once for all of its output partitions. A hash repartition concatenates its input and scatters it in a single operation, and that time is billed to p0 — so p0 standing far above its siblings is the accounting, not skew. Sub-lines sum to their node line either way. Written whether or not this plan has a repartition.
build_profile=benchmarks opt-level=3
allocator=rmm-pool initial=113.4GiB max=126.7GiB of 133.4GiB free on a discrete device
shared_work_charged_to=p0
sync_floor_us=1
nodes_at_or_below_floor=8/25
nodes_total_us=66148
total_us=71568
